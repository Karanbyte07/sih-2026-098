# Kinetica Platform
Aerospace telemetry and digital twin platform.

## Structure
- `frontend/` — React + Vite UI
- `backend/` — Python FastAPI server

## Frontend
```bash
cd frontend && npm install && npm run dev
```

## Backend
```bash
cd backend && pip install -r requirements.txt && uvicorn main:app --reload
```
